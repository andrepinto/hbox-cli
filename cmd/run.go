package cmd

/*


import (
	"context"
	"fmt"
	"github.com/andrepinto/hbox-cli/internal/functions"
	"os"
	"os/signal"
)

func main() {
	//os.Setenv("INTERNAL_IMAGE_REGISTRY", "docker.io")
	ctx, _ := signal.NotifyContext(cmd.Context(), os.Interrupt)
	err := functions.Run(context.Background(), "data", "", false)
	fmt.Println(err)
}
*/
